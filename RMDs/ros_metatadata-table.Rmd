---
output: html_fragment
---

## Table `r db_table$table()`

#### Description

`r ifelse(is.na(db_table$table_comment()), '<p class="error">Not filled</p>', paste0('*', db_table$table_comment(), '*'))`

#### Columns

```{r}
db_table_columns <- db_table$columns()
out_table_columns(db_table_columns)
db_code_list_or_registries <- db_table_columns[db_metadata$is_column_code_list_function()(foreign_key)]
db_data_dependencies <- db_table_columns[db_metadata$is_column_data_function()(foreign_key)]
```
```{r results = "asis"}
if (nrow(db_code_list_or_registries) > 0) {
  cat("#### Code lists or Ros registries")
}
```
```{r}
if (nrow(db_code_list_or_registries) > 0) {
  out_table_code_list(db_code_list_or_registries)
}
```
```{r results = "asis"}
if (nrow(db_data_dependencies) > 0) {
  cat("#### Data dependencies")
}
```
```{r}
if (nrow(db_data_dependencies) > 0) {
  out_table_data_dependencies(db_data_dependencies)
}
```